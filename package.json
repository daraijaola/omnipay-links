{
  "name": "omnipay-links",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@ston-fi/omniston-sdk": "^0.8.3",
    "@tonconnect/ui": "^2.4.4"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^8.0.16"
  }
}
